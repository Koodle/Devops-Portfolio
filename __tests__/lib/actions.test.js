import '@testing-library/jest-dom'
import { render, screen } from '@testing-library/react'

describe('MyComponent', () => {
    it.todo('should render correctly'); // This will not run and will be marked as a todo
});
